export const metadata = {
  title: "Admin Control Center | Ahaaq Auto Exchange",
  robots: {
    index: false,
    follow: false,
    nocache: true,
  },
};

export default function AdminLayout({ children }) {
  return children;
}
